const num1 = 100;
const num2 = 60;

let val;

//Simple Math
val = num1 + num2;
val = num1 * num2;
val = num1 % num2;

//Math Object
val = Math.PI;
val = Math.E;

val = Math.round(2.85);
val = Math.ceil(2.35);
val = Math.floor(2.85);
val = Math.sqrt(64);
val = Math.abs(-3);
val = Math.pow(3, 2);
val = Math.min(2, 33, 4, 1, -2);
val = Math.max(2, 33, 4, 1, -2);
val = Math.random();

val = Math.random() * 20; //return decimal between 0 and 20

val = Math.floor(Math.random() * 20); // returns an integer between 0 and 20

console.log(val);
